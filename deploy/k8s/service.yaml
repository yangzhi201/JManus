apiVersion: v1
kind: Service
metadata:
  name: jmanus
  namespace: default
  labels:
    app: jmanus
spec:
  type: ClusterIP
  selector:
    app: jmanus
  ports:
    - name: http
      port: 18080
      targetPort: 18080
      protocol: TCP
